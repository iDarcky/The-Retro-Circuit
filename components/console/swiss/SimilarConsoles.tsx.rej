--- SimilarConsoles.tsx
+++ SimilarConsoles.tsx
@@ -89,8 +89,8 @@
         setSimilarConsoles([]);
       }
     }
-
-  }, [currentConsole.id, currentConsole.specs?.price_launch_usd, currentConsole.variants, isMobile]);
+    // eslint-disable-next-line react-hooks/exhaustive-deps
+  }, [currentConsole.id, currentConsole.specs?.price_launch_usd, currentConsole.variants]);

   // Add horizontal scrolling behavior for desktop
   useEffect(() => {
